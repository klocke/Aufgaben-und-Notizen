<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/action_filter_items"
        android:checkable="true"
        android:title="@string/title_action_filter"
        app:showAsAction="always">
        <menu>
            <item
                android:id="@+id/action_filter_items_all"
                android:checkable="true"
                android:checked="false"
                android:title="Alle" />
            <item
                android:id="@+id/action_filter_items_todos"
                android:checkable="true"
                android:checked="true"
                android:title="Aufgaben" />
            <item
                android:id="@+id/action_filter_items_notes"
                android:checkable="true"
                android:checked="false"
                android:title="Notizen"/>
        </menu>
    </item>

    <item
        android:id="@+id/action_add_item"
        android:icon="@drawable/ic_add"
        android:title="@string/title_action_add_item"
        app:showAsAction="always" />

    <item
        android:id="@+id/action_settings"
        android:title="@string/title_action_settings"
        app:showAsAction="never" />

</menu>